<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nouvel Incident - Jurassic Park</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('partials/header') %>

    <main>
        <h1>🚨 Déclarer un nouvel incident</h1>
        
        <div style="background-color: #fff3cd; border: 1px solid #ffeaa7; padding: 1rem; border-radius: 4px; margin-bottom: 2rem;">
            <strong>⚠️ Attention:</strong> En cas d'urgence critique, contactez immédiatement l'équipe de sécurité du parc !
        </div>
        
        <form id="incident-form" method="POST" action="/api/incidents">
            <div class="form-group">
                <label for="title">Titre de l'incident*</label>
                <input type="text" id="title" name="title" required placeholder="Ex: Évasion de Vélociraptor - Secteur Nord">
            </div>
            
            <div class="form-group">
                <label for="description">Description détaillée</label>
                <textarea id="description" name="description" placeholder="Décrivez l'incident en détail..."></textarea>
            </div>
            
            <div class="form-group">
                <label for="type">Type d'incident*</label>
                <select id="type" name="type" required>
                    <option value="">Sélectionnez un type</option>
                    <% types.forEach(type => { %>
                        <option value="<%= type %>"><%= type %></option>
                    <% }); %>
                </select>
            </div>
            
            <div class="form-group">
                <label for="zone">Zone concernée*</label>
                <select id="zone" name="zone" required>
                    <option value="">Sélectionnez une zone</option>
                    <% zones.forEach(zone => { %>
                        <option value="<%= zone %>"><%= zone %></option>
                    <% }); %>
                </select>
            </div>
            
            <div class="form-group" id="urgency-field">
                <label for="urgency">Niveau d'urgence*</label>
                <select id="urgency" name="urgency" required>
                    <option value="">Sélectionnez un niveau</option>
                    <% urgencyLevels.forEach(level => { %>
                        <option value="<%= level.toLowerCase() %>"><%= level %></option>
                    <% }); %>
                </select>
                <small style="color: #666; font-size: 0.9em;">
                    Note: Les évasions et blessures ont des niveaux d'urgence prédéfinis
                </small>
            </div>
            
            <div style="margin-top: 2rem;">
                <button type="submit" class="btn">🦕 Enregistrer l'incident</button>
                <a href="/" class="btn cancel">Annuler</a>
            </div>
        </form>
    </main>

    <%- include('partials/footer') %>
    <script src="/js/main.js"></script>
</body>
</html>